# App Review Information

Keep App Store review contact details, demo users, demo passwords, phone numbers, and notes out of git.

For metadata uploads, create the real files locally under:

`ios/fastlane/metadata/review_information/`

Fastlane will read that directory when present, but it is ignored by git.
